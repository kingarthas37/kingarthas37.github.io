'use strict';

var gulp = require('gulp');
var clean = require('gulp-clean');

// Clean css,js min files
gulp.task('clean', function () {
    return gulp.src(['public/css/min','public/js/min'], {read: false}).pipe(clean());
});